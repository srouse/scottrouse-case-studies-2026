PORTFOLIO_PASSWORD=change-me
SESSION_COOKIE_NAME=portfolio_session

# Optional: bump when you need a new Next.js build ID (helps bust CDN/cache for static assets after `next build`).
# CACHE_REVISION=2
